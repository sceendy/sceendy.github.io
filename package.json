{
  "name": "sceendy.com",
  "description": "Cindy Juarez personal site",
  "version": "2.0.0",
  "author": "Cindy Juarez <sceendy@gmail.com>",
  "dependencies": {
    "gatsby": "^1.9.145",
    "gatsby-link": "^1.6.32",
    "gatsby-plugin-google-analytics": "^1.0.14",
    "gatsby-plugin-react-helmet": "^1.0.8",
    "gatsby-remark-prismjs": "^1.2.11",
    "gatsby-source-filesystem": "^1.5.11",
    "slug": "^0.9.1"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "deploy": "gatsby build --prefix-paths && cp ./CNAME public/ && gh-pages -d public --branch master",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write \"src/**/*.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gatsby-transformer-remark": "^1.7.26",
    "gh-pages": "^1.1.0",
    "prettier": "^1.9.2"
  }
}
